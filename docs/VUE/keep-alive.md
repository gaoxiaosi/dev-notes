# keep-alive

------

### 作用：

如果需要在组件切换时，保存组件状态防止多次渲染，可使用keep-alive对组件进行包裹。简单来说，缓存组件，提高性能。

### 属性：

- `include`：字符串或正则，只有名称匹配的组件会被缓存。
- `exclude`：字符串或正则，任何名称匹配的组件都不会被缓存。
- `max`：数字，最多可以缓存组件数。

### 使用场景：

列表页→详情页，在activated中判断两次的id是否同一个，如果不同再重新请求数据。